AniList Taste Analyzer 1.9.4

Hotfix
- Restored AniList's documented CharacterEdge.voiceActors cast source.
- Japanese and English are classified locally using Staff.languageV2.
- Franchise-level recurrence remains: connected seasons count once.
- If AniList credits multiple performers of the same language to one character,
  the report labels that as "Multiple credited performers" rather than guessing
  which age, version, or special circumstance each actor represents.
- VA cache schema is version 4, so the broken v1.9.3 cache rebuilds automatically.
